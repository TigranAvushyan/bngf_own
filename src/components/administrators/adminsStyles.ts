import { ButtonStyle } from '../ui/buttons/buttonStyles';
import { SCREEN_PADDING } from '../../style/global.style';
import { StyleSheet } from 'react-native';

export const adminsButtonStyle: ButtonStyle = {
  container: {
    marginHorizontal: SCREEN_PADDING,
    marginBottom: 20,
  },
};

export const adminsStyles = StyleSheet.create({});
