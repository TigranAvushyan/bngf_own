import React, { FC } from 'react';
import { View } from 'react-native';

interface InputAreaPropsType {
  changeable?: boolean;
}

const InputArea: FC<InputAreaPropsType> = ({}) => {
  return (
    <View>

    </View>
  );
};


export default InputArea;
